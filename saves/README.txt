SenBoy saves/
This is the folder where battery backed RAM is saved.
Filename format 'NAME_C0_C001.sav' where NAME is the name of the ROM as designated in its header, C0 is the header checksum and C001 is the entire ROM checksum (both in hexadecimal).