language: c++

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

before_install:
  - if [ $TRAVIS_OS_NAME == linux ]; then sudo apt-get install libsfml-dev; fi
  - if [ $TRAVIS_OS_NAME == osx ]; then brew update && brew install sfml; fi

script: 
  - cmake .
  - make
